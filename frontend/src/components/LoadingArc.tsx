// ─────────────────────────────────────────────
// LoadingArc — 회전 호(arc) 로딩 인디케이터
//
// uiux 3 (운세 로딩), uiux 11~14 (꽃 로딩) 중앙 원형 인디케이터에 사용
// 닫히지 않은 호(arc) 2개가 반대 방향으로 회전하는 CSS 애니메이션
//
// Props:
//   size?: number            — 지름(px), 기본 160
//   color?: string           — 호 색상, 기본 '#76d4ff'
//
// 구현:
//   SVG circle + stroke-dasharray/stroke-dashoffset 또는
//   CSS border + border-radius + @keyframes rotate
// ─────────────────────────────────────────────

// TODO: LoadingArc 구현
// interface Props { size?: number; color?: string }
// export default function LoadingArc({ size = 160, color = '#76d4ff' }: Props) { ... }
