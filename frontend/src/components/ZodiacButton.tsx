// ─────────────────────────────────────────────
// ZodiacButton — 별자리 선택 버튼
//
// uiux 2 ZodiacSelect 화면에서 12개 그리드로 렌더링
//
// 구조:
//   [순위 번호] [원형 아이콘 (#71fffd border)] [한글 이름] [날짜범위]
//   전체: pill 형태 GlassCard, 클릭 시 selected 상태 하이라이트
//
// Props:
//   meta: ZodiacMeta         — 별자리 메타 (이름, 날짜, 아이콘, 순위)
//   selected?: boolean       — 선택 상태 스타일링
//   onClick: (id: ZodiacSign) => void
// ─────────────────────────────────────────────

// TODO: ZodiacButton 구현
// interface Props { meta: ZodiacMeta; selected?: boolean; onClick: (id: ZodiacSign) => void }
// export default function ZodiacButton({ meta, selected, onClick }: Props) { ... }
