// ─────────────────────────────────────────────
// FortuneCircle — 운세 원형 게이지 카드
//
// uiux 4/6/7/15에서 관계 운 / 금전 운 / 업무 운 표시에 사용
// SVG arc로 퍼센트 게이지 표현, 중앙에 수치 텍스트
//
// Props:
//   type: FortuneType        — 'relationship' | 'money' | 'work'
//   score: number            — 0~100
//   size?: 'sm' | 'lg'       — sm: FortuneBadge(uiux 15 우상단), lg: 결과 화면 메인
//
// 색상은 FORTUNE_COLORS[type] 참조
// 레이블은 FORTUNE_LABELS[type] 참조
// ─────────────────────────────────────────────

// TODO: FortuneCircle 구현
// import { FORTUNE_COLORS, FORTUNE_LABELS } from '../constants/zodiacs';
// interface Props { type: FortuneType; score: number; size?: 'sm' | 'lg' }
// export default function FortuneCircle({ type, score, size = 'lg' }: Props) { ... }
