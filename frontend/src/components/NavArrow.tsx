// ─────────────────────────────────────────────
// NavArrow — 화면 간 이동 화살표 네비게이션
//
// uiux 4/6/7/8/9/10 하단 좌우에 사용
// "← 다시 선택하기" / "행운 요소 확인하기 →" 형태
//
// Props:
//   direction: 'left' | 'right'
//   label: string
//   to: string               — React Router 이동 경로
//   opacity?: number         — 비활성 느낌 줄 때 (기본 0.6)
// ─────────────────────────────────────────────

// TODO: NavArrow 구현 (React Router Link 사용)
// interface Props { direction: 'left' | 'right'; label: string; to: string; opacity?: number }
// export default function NavArrow({ direction, label, to, opacity = 0.6 }: Props) { ... }
